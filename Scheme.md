Phase A：产品化基础（先于算法）
目标：把 demo 变成“可持续迭代系统”。

接口契约固化
固化 process_frame 请求/响应 schema（字段、版本、错误码、超时语义）
增加 request_id、model_version、trace_id
API 向后兼容（v1/v2）
可观测性
统一日志格式（client/server/ai）
指标：FPS、端到端延迟、AI推理耗时、丢帧率、失败率
最少先做本地 dashboard + 日志轮转
配置体系
把“模型路径、阈值、服务地址、开关”从代码移到配置
支持 dev/test/prod 环境配置
测试与回归
API 单测、关键链路集成测试、1段固定视频回归测试
先实现“同样输入，输出行为不退化”的自动检查
验收标准：你能稳定回答“慢在哪里、错在哪里、是否变差了”。

Phase B：后端与数据库（业务骨架）
目标：从单机 demo 进入真正“多用户会议系统”。

Meeting Server 最小可用
用户登录（可先简化）
房间创建/加入/离开
基本 signaling（哪怕先不传媒体）
数据库落地（建议 PostgreSQL）
核心表：users, rooms, participants, sessions, audit_logs
AI 相关表：model_registry, inference_records（用于可追溯）
用迁移工具管理 schema（而不是手改）
状态与事件
“加入会议”“模型切换”“隐私级别变更”都做事件记录
方便审计和问题复盘
验收标准：多用户房间流程稳定、关键动作有持久化记录。

Phase C：YOLO 上线（先检测，后识别）
目标：把“看见目标”能力稳定引入实时链路。

AI Service 模块化推理管线
decode -> preprocess -> detect -> postprocess -> encode
YOLO 先单模型、单输入尺寸，保证实时性
性能策略
可调推理分辨率
跳帧推理（每 N 帧检测一次）
检测结果时序平滑（减少框抖动）
回退机制
YOLO 出错时自动降级（透传原帧/轻处理）
不影响会议主链路可用性
验收标准：目标检测接入后，端到端延迟和帧率仍在可用区间。

Phase D：ArcFace 上线（识别层）
目标：身份相关能力可控上线。

人脸流程拆分
face detect / align / embedding / match 分离
ArcFace 只负责 embedding，检索与阈值策略单独管理
向量库/特征存储
MVP 可先 PostgreSQL + pgvector（或本地索引）
明确阈值、误识别处理、重试策略
隐私与合规机制
embedding 加密存储（至少传输加密 + 存储加密）
提供“删除身份数据”能力
审计谁在何时触发了识别流程
验收标准：识别准确率可量化，误识别可追踪可回滚。

Phase E：对抗扰动模型（隐私核心）
目标：在真实会议里做到“可控隐私保护”。

策略化，而不是单模型硬编码
模式开关：none / blur / perturbation / hybrid
按用户/会议/角色动态选择策略
质量平衡
指标同时看：隐私强度、识别抑制效果、视觉可接受度、实时性
做 A/B：扰动前后人脸可识别率下降曲线
鲁棒性验证
不同光照、角度、压缩率、分辨率下效果一致性
与 YOLO/ArcFace 共存时的冲突测试（先后顺序很关键）
验收标准：隐私效果可证明，用户体验不过度受损。

Phase F：端云协同（最后上）
目标：把计算智能分层，适配不同设备能力。

推理路由器
决策维度：设备性能、带宽、延迟、隐私等级、电量
路由策略：edge-only / cloud-only / hybrid
任务切分
端侧：轻量预处理、简单检测、缓存与降级
云侧：重模型、批量识别、策略编排
结果回传带版本号与时序戳
稳定性机制
网络抖动时自动切回端侧简化模型
云不可用时保证会议不中断
验收标准：网络和算力变化下，系统自动切换且用户无明显中断。