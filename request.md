# SecureMeeting 设计原则（从 Demo 到产品）

## 0. 总体目标
SecureMeeting 必须以“可持续迭代”为第一目标：先保证系统可运行、可观测、可回滚，再逐步引入复杂能力（YOLO、ArcFace、对抗扰动、端云协同）。禁止一次性堆叠全部功能。

---

## 1. 演进顺序（必须遵守）
1. 工程化底座（接口、日志、配置、测试）
2. 后端与数据库（用户/房间/会话/审计）
3. YOLO 检测
4. ArcFace 识别
5. 对抗扰动
6. 端云协同

禁止跳过前置阶段直接进入后置阶段。

---

## 2. 架构分层原则
系统分为独立模块，模块必须可单独开发、测试、运行：
- Client（C++/Qt/OpenCV）：采集、渲染、交互、调用服务
- AI Service（Python/FastAPI/PyTorch）：检测、识别、扰动推理
- Meeting Server（Python）：用户、房间、信令、会话控制
- Database（PostgreSQL 等）：业务数据、模型元数据、审计数据

要求：
- Client 不实现模型算法细节
- AI Service 不持有会议业务状态
- Meeting Server 不耦合具体模型实现

---

## 3. 接口与兼容性原则
### 3.1 API 契约稳定
- 所有接口必须版本化（如 `/api/v1/...`）
- 请求/响应结构必须文档化且可测试
- 新字段默认可选，避免破坏旧客户端
- 错误码体系统一（业务错误、鉴权错误、系统错误）

### 3.2 可追踪性
每次请求必须可追踪：
- `request_id`
- `trace_id`
- `model_version`
- `policy_version`

---

## 4. 算法可插拔原则
算法能力必须通过抽象接口接入，禁止业务层直接依赖具体模型类名。

推荐抽象：
- `Detector`（检测）
- `Recognizer`（识别）
- `Protector`（隐私扰动）
- `InferencePipeline`（编排）

要求：
- 支持 mock 与真实模型无缝替换
- 支持多模型并存与灰度切换
- 支持模型失败时降级到安全路径（如透传/模糊）

---

## 5. 数据模型扩展原则
数据库设计必须为后续阶段预留：
- 用户、房间、参与者、会话
- 模型注册表（名称、版本、状态、发布时间）
- 策略配置（识别开关、扰动等级、端云路由策略）
- 推理记录与审计日志（可追溯、可合规）

要求：
- 使用迁移工具管理 schema
- 不允许手工改表绕过迁移
- 历史数据可兼容读取

---

## 6. 策略与配置中心原则
“是否检测/识别/扰动、在哪侧推理”必须配置化，禁止硬编码在业务逻辑中。

至少支持：
- 环境配置（dev/test/prod）
- 会议级策略
- 用户级策略
- 动态开关（feature flag）

---

## 7. 性能与实时性原则
实时体验是硬约束，新增能力不能无上限侵占时延。

核心指标：
- FPS（目标 20–30，最低可接受 15）
- 端到端延迟（P95）
- 推理耗时（按模型拆分）
- 丢帧率、错误率、恢复时间

要求：
- UI 线程不可阻塞
- 支持降采样/跳帧/异步队列
- 支持超时与背压控制

---

## 8. 可观测与可运维原则
每个模块必须具备：
- 结构化日志
- 指标上报
- 健康检查（health/readiness）
- 告警与故障定位信息

任何上线问题必须能快速回答：
1) 错在哪一层  
2) 慢在哪一段  
3) 是否可立即回滚

---

## 9. 安全与隐私原则
- 全链路传输加密（TLS）
- 鉴权与权限控制
- 生物特征/embedding 加密存储
- 最小化采集与最短保留
- 可审计、可删除、可追责

对抗扰动属于隐私增强能力，必须可开关、可评估、可回退。

---

## 10. 端云协同原则
端云协同是后期能力，不得前置。

路由决策依据：
- 设备算力
- 网络带宽/抖动
- 延迟预算
- 隐私等级
- 能耗约束

要求：
- 端侧与云侧都必须有兜底路径
- 云不可用时系统可降级运行
- 策略切换对用户尽量无感

---

## 11. 测试与发布原则
每阶段必须有明确 DoD（完成定义）：
- 功能正确
- 性能达标
- 回归通过
- 观测完备
- 可灰度、可回滚

发布策略：
- 小步快跑，分批灰度
- 先内部、后外部
- 失败自动回退

---

## 12. 禁止事项
- 禁止一次性上齐所有高级能力
- 禁止在前置阶段未稳定时引入下一阶段核心复杂度
- 禁止跨层直接调用破坏边界
- 禁止把策略写死在代码中
- 禁止不可回滚的发布

---

## 13. AI 执行指令（给实现型 AI）
当 AI 参与设计/编码时，必须遵守：
1. 优先保证分层清晰与接口稳定，而不是短期功能堆叠  
2. 所有新增能力必须提供：配置开关、监控指标、错误处理、降级路径  
3. 若某项改动会破坏兼容性，先给迁移方案，再执行  
4. 任何阶段完成前，输出该阶段验收清单与风险清单  
5. 如需求冲突，优先级为：稳定性 > 安全隐私 > 可维护性 > 功能完整性